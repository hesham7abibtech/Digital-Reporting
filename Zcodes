# 1. Elevate Execution Policy (Run this ONCE in Admin PowerShell)
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 2. Optimized Deployment Pipeline (Native Environment)
# Ensures .next and .vercel artifacts are purged for Edge compatibility
Remove-Item -Recurse -Force app\.next, app\.vercel, app\out -ErrorAction SilentlyContinue

# 3. Synchronize Master Repository
git add -A
git commit -m "feat: infrastructure and auth stabilization"
git push

# 4. Standardized Cloudflare Deployment
# Uses the package.json native scripts to ensure Cloudflare Edge compatibility
cd app
npm run pages:deploy
cd ..
